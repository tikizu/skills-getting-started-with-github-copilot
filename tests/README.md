# Tests

This directory contains backend FastAPI tests for the `src/app.py` API.

## Run tests

From the repository root:

```bash
pytest
```

## Notes

- Tests use `pytest` and `fastapi.testclient`
- The app is imported from `src.app`
- New tests should be added to this directory
